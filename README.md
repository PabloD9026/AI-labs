# Тема
Методы искусственного интеллекта. EDA. Линейная регрессия. 
Дерево решений. CatBoost. XGBoost. Нейронный сети (MLP)

## Цель работы
Целю данной работы заключается в получении навыков анализа первичных данных и 
определение признаков взаимосвязи (EDA), понимания моделей: линейная регрессия, 
дерево решений, CatBoost, XGBoost, нейронные сети (MLP) и умения разрабатывать 
программу на языке Python для реализации представленных моделей.

## Задачи
1.	Описать датасета и определить влияние признаков и выбрать признаки, которые наиболее подходят для поставленной задачи предсказания (EDA).
2.	Построить пайплйан (DVC) исходя из результатов EDA.
3.	Реализовать линейную регрессию, определить весы, метрики и ошибки.
4.	Реализовать дерево решений, определить метрики и ошибки. Привести рисунок первых узлов дерева решений.
5.	Реализовать CatBoost, определить метрики и ошибки. Выгрузить Feature Importance.
6.	Реализовать XGBoost, определить метрики и ошибки. Выгрузить Feature Importance.
7.	Реализовать нейронную сети, определить метрики, ошибки, кривые обучения, гимтограммы весов с интерпретацией и график из Tensorboard.
8.	Выгрузить конечный вычислительный граф DVC.
9.	Построить сводную таблицу с метриками и сделать вывод какая модель отработала лучше и почему.


## Описание и анализ датасета
### Контекст

Этот набор данных содержит информацию о подержанных автомобилях. Он разделен на три файла формата .csv. 

# Ход работы
## Начальная обработка данных ([Код](eda.ipynb))
Этот проект объединяет несколько наборов данных об автомобилях в один согласованный файл, кодирует категориальные признаки, нормализует числовые значения и разделяет данные на обучающую и тестовую выборки. Ниже подробно описан каждый шаг и используемые библиотеки.

## Используемые библиотеки

- **pandas** – загрузка, обработка и объединение данных.
- **scikit-learn** – для LabelEncoder (кодирование меток), train_test_split (разделение на обучающую/тестовую выборки) и MinMaxScaler (масштабирование).
- **numpy**, **matplotlib**, **seaborn** – (опционально, показаны в исследовательском анализе) для численных операций и визуализации.

## Пошаговый процесс

### 1. Объединение данных

Загружаются три исходных CSV-файла:
- `Car_data.csv`
- `Car_details_v3.csv`
- `CAR_DETAILS_FROM_CAR_DEKHO.csv`

Первый набор данных (`Car_data.csv`) имеет другие названия столбцов, поэтому его столбцы переименовываются в соответствии со схемой третьего набора (`CAR_DETAILS_FROM_CAR_DEKHO.csv`). Общие столбцы, которые остаются:

- `name` – модель автомобиля
- `year` – год выпуска
- `selling_price` – цена продажи (в индийских рупиях)
- `km_driven` – пробег в километрах
- `fuel` – тип топлива (бензин, дизель, CNG и т.д.)
- `seller_type` – тип продавца (частное лицо или дилер)
- `transmission` – коробка передач (механика или автомат)
- `owner` – количество предыдущих владельцев

Все три датасета объединяются по строкам в один DataFrame и сохраняются как `merged.csv`.

### 2. Кодирование категориальных признаков

Поскольку модели машинного обучения требуют числовых входных данных, все категориальные столбцы (тип object) преобразуются с помощью **LabelEncoder** (`sklearn.preprocessing.LabelEncoder`). Каждой уникальной категории ставится в соответствие целое число (0, 1, 2, …). Закодированный датасет сохраняется как `encoded.csv`.

### 3. Нормализация

Закодированный датасет нормализуется с помощью **MinMaxScaler** (`sklearn.preprocessing.MinMaxScaler`). Он приводит каждый признак к диапазону [0, 1] по формуле:

X_scaled = (X - X_min) / (X_max - X_min)


Нормализация гарантирует, что ни один признак не будет доминировать над другими из-за разных единиц измерения или масштаба. Полный нормализованный датасет сохраняется как `normalized.csv` – этот файл используется для дальнейшего анализа и визуализации.

### 4. Разделение на обучающую и тестовую выборки

Нормализованный датасет случайным образом перемешивается и разделяется на **80% обучающей** и **20% тестовой** выборок с помощью `train_test_split` и параметра `random_state=42` для воспроизводимости результатов. Разделенные данные сохраняются как:
- `train.csv`
- `test.csv`

## Итоговые файлы

| Файл               | Содержание                                                                 |
|--------------------|----------------------------------------------------------------------------|
| `merged.csv`       | Необработанные объединенные данные (категориальные тексты присутствуют).    |
| `encoded.csv`      | Категориальные столбцы преобразованы в целые числа.                        |
| `normalized.csv`   | Все признаки масштабированы к [0,1] с помощью MinMaxScaler – для EDA.       |
| `train.csv`        | 80% нормализованных, перемешанных данных – для обучения модели.            |
| `test.csv`         | 20% нормализованных, перемешанных данных – для оценки модели.              |


### 5. Общий анализ данных

После завершения предобработки (объединения, кодирования и нормализации) был проведён общий анализ итогового набора данных `normalized.csv` для проверки корректности преобразований.

**Основные проверки:**
- Все ли столбцы имеют числовой тип данных (float64)?
- Есть ли пропущенные значения?
- Каково статистическое распределение признаков?

#### Результаты:

- **Типы данных и пропуски**  
  Все 8 столбцов имеют тип `float64`. Пропущенные значения отсутствуют (0 в каждом столбце).


- **Краткая статистика**  

Поскольку все признаки были нормализованы в диапазон [0, 1], значения находятся между 0 и 1. Ниже приведены основные статистики:

| Признак        | mean    | std     | min | 25%    | 50%    | 75%    | max |
|----------------|---------|---------|-----|--------|--------|--------|-----|
| name           | 0.5129  | 0.2644  | 0.0 | 0.2968 | 0.5316 | 0.6879 | 1.0 |
| year           | 0.8259  | 0.1109  | 0.0 | 0.7568 | 0.8378 | 0.9189 | 1.0 |
| selling_price  | 0.0576  | 0.0726  | 0.0 | 0.0225 | 0.0400 | 0.0650 | 1.0 |
| km_driven      | 0.0287  | 0.0227  | 0.0 | 0.0144 | 0.0254 | 0.0381 | 1.0 |
| fuel           | 0.6023  | 0.3761  | 0.0 | 0.2500 | 0.2500 | 1.0000 | 1.0 |
| seller_type    | 0.4226  | 0.2141  | 0.0 | 0.5000 | 0.5000 | 0.5000 | 1.0 |
| transmission   | 0.8791  | 0.3261  | 0.0 | 1.0000 | 1.0000 | 1.0000 | 1.0 |
| owner          | 0.5330  | 0.1919  | 0.0 | 0.4286 | 0.4286 | 0.7143 | 1.0 |

- **Вывод:**  
Датасет полностью готов для дальнейшего исследовательского анализа и построения моделей машинного обучения. Все признаки числовые, масштабированы от 0 до 1, пропусков нет.


### 6. Корреляционный анализ

Для выявления взаимосвязей между признаками была построена матрица корреляции (коэффициент Пирсона).  
Несмотря на то что все признаки уже нормализованы и имеют числовой тип (`float64`), на всякий случай была выполнена повторная проверка: все столбцы приведены к числовому виду, и для них вычислена корреляция.

## Результаты и интерпретация

<img width="760" height="713" alt="image" src="https://github.com/user-attachments/assets/9c3601fd-2f34-4303-b988-930163ae298d" />

Цена продажи (selling_price) сильнее всего положительно коррелирует с годом выпуска (year)
Коэффициент корреляции ≈ 0.61. Это ожидаемо: чем новее автомобиль, тем выше его цена.
Отрицательная корреляция с пробегом (km_driven) – около -0.37.
Чем больше километраж, тем ниже стоимость (логично, износ снижает цену).

Отрицательная корреляция с количеством владельцев (owner) – примерно -0.46.
Автомобили, у которых было больше владельцев, в среднем дешевле (доверие покупателя к «одноруким» автомобилям выше).

Отрицательная корреляция с типом коробки передач (transmission) – около -0.43.
Здесь важно помнить, что после нормализации Automatic может быть закодирован как 1, а Manual как 0 (или наоборот). Знак корреляции говорит о том, что автоматическая трансмиссия связана с более низкой ценой (в данном датасете, вероятно, преобладают бюджетные автомобили с автоматом или выборка смещена). В реальности этот эффект требует дополнительной проверки.

Корреляция с названием модели (name) очень слабая (около 0.05).
Это означает, что само по себе название модели почти не объясняет цену – гораздо важнее её числовые характеристики (год, пробег и т.д.). Причина в том, что name после кодирования стал произвольным числом, не несущим информации о классе автомобиля (премиум/бюджетный). Для учёта модели потребовалось бы создавать отдельные категориальные признаки (one-hot encoding).

Results 

Path                          MAE      MSE      R2
models\metrics_linear.json    0.03063  0.00321  0.43015
models\metrics_tree.json      0.01541  0.0012   0.78706
models\metrics_catboost.json  0.0133   0.00083  0.85315
models\metrics_xgboost.json   0.00922  0.00056  0.89975
models\metrics_nn.json        0.0223   0.00161  0.71334
